#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        map<int,int>mpp;
        vector<int> arr,ans;
        for(int i=0;i<n;i++){
            int k;
            cin>>k;
            arr.push_back(k);
            mpp[k]++;
        }
        for(auto &it:mpp){
            ans.push_back(it.first);
            it.second--;
        }
        for(auto &it:mpp){
            while(it.second--)
                ans.push_back(it.first);
        }
        for (auto it:ans){
            cout<<it<<" ";
        }
        cout<<endl;
    }
}