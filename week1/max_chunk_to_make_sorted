class Solution {
public:
    int maxChunksToSorted(vector<int>& arr) {
        int n=arr.size();
        int chunk=0,sum1=0,sum2=0;
        for(int i=0;i<n-1;i++){
           sum1+=i;
           sum2+=arr[i];
           if(sum1==sum2)
            chunk+=1;
        }
        return chunk+1;
    }
};